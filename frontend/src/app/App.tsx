export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">SecretManager</p>
        <h1>Secrets dashboard bootstrap</h1>
        <p>
          Phase 1 scaffolding is in place for the React frontend, Azure Functions
          API, and the shared test projects.
        </p>
      </section>
    </main>
  );
}
